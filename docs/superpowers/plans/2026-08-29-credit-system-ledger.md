# SDD ledger — plan: docs/superpowers/plans/2026-08-28-credit-system.md

Worktree: .claude/worktrees/credit-system, branch worktree-credit-system, base cdf92197 (local main).
Spec: docs/superpowers/specs/2026-08-28-credit-system-design.md (binding authority).
TEST_DATABASE_URL=postgresql://suyash@localhost:5432/credit_system_test
 (local Homebrew postgresql@18 on 5432, pgvector 0.8.6 built from source, all 68 baseline migrations applied, 81 tables, actor_type enum present).

## Pre-flight scan

| Rows checked | Finding |
|---|---|
| T1 -> T2 (tables, migrations/meta/_journal.json) | Clean. T1 generates, T2 hand-registers its function migration after it. |
| T2 -> T3 (credits package scaffold) | T3 cannot run before T2: T2 Step 1 creates package.json/vitest.config. The handoff's "Tasks 1/3/6 need no DB" grouping is not a valid dispatch order. Sequential order 1..15 stands. |
| T3 -> T4 (costMicro) | Clean. |
| T4 -> T5 (read.ts isUnlimited stub then real) | Clean, declared in both task texts. |
| T4 -> T6, T4 -> T8 (resolveRate into agent-orchestrator) | CONFLICT. Neither task lists a package.json edit adding @serverless-saas/credits to apps/agent-orchestrator, and resolveRate is Drizzle-based while the orchestrator uses a raw pg pool. See Ruling R5. |
| T5 -> T11 (seeds/features.ts) | CONFLICT (cosmetic). T5 Step 2's heading says "retire the two it replaces"; its body says leave them for T11. See Ruling R4. |
| T5 -> T6 (rate rows seeded before cost.ts reads them) | Clean, order correct. |
| T4,T5 -> T7 (grantCredits, credits entitlement) | Clean. |
| T8 -> T9 -> T10 (apps/agent-orchestrator/src/credits.ts and its test) | Clean, strictly additive and sequential. |
| T8,T9,T10 -> T11 (quota call sites gone before deletion) | Clean; T11 Step 1 greps to verify. |
| T4,T5 -> T12 (getBalance/getLedger/grantCredits, permissions seed) | Clean. |
| T12 -> T13, T12 -> T15 | Clean. |
| T14 (worker) | Independent. |
| Each task's self-consistency (tests vs code vs files) | Clean except T5 (heading/body, R4) and T6/T8 (missing dep, R5). |
| Plan mandate vs review rubric | No task mandates an assertion-free test or verbatim duplication. |
| Test-DB task list | Plan section lists 2,4,7,12,14; handoff lists 2,4,5,7,12,13,14. Moot — a database is available for all of them. |

## Rulings

Ruling R1 (database): local Postgres, not Supabase dev. User chose it. TEST_DATABASE_URL as above. Cost if wrong: none outside this machine.
Ruling R2 (economics): 1 credit = 1 US cent at cost, no markup; plan allowances free 200 / starter 2,000 / business 10,000 / enterprise unlimited. User confirmed. Cost if wrong: reseed credit_rates and plan_entitlements — data, no code change.
Ruling R3 (fail mode): credit pre-check fails CLOSED on DB error. User confirmed. Cost if wrong: a credits DB outage blocks chat.
Ruling R4 (T5/T11 features.ts): T5 Step 2's body governs — add the `credits` feature only, leave `messages` and `llm_tokens` in place; T11 Step 3 deletes them. The heading is stale wording. Cost if wrong: none; T11 verifies the end state.
Ruling R5 (orchestrator dependency): the Task 6 implementer adds `@serverless-saas/credits` to apps/agent-orchestrator/package.json as a workspace dependency. If the Drizzle-backed `resolveRate` cannot be used from the orchestrator process (it holds a raw pg pool, not a Drizzle client), implement rate lookup over that existing pool inside apps/agent-orchestrator/src/credits.ts and import only the pure `costMicro`/`MICRO_PER_CREDIT` from the package. Cost if wrong: rework inside T6/T8 only.
Ruling R6 (env safety): every drizzle-kit / seed / script invocation must set DATABASE_URL explicitly to TEST_DATABASE_URL. drizzle.config.ts falls back to apps/api/.env, whose DATABASE_URL points at the shared Supabase dev database. Cost if wrong: credit migrations land on dev early.

## Progress
Task 1: implemented (commit b21ebc8f), review dispatched. Implementer deviation: `.default(0n)` crashes drizzle-kit 0.28.1 (BigInt in JSON.stringify during snapshot diff); substituted `.default(sql`0`)`, emitted DDL verified identical.
Task 1: minor (deferred): add an in-file comment at schema/credits.ts:9,15 explaining why the bigint defaults use sql`0` instead of 0n (drizzle-kit 0.28.1 crash), so a future editor does not revert it.
Task 1: complete (commits cdf9219..b21ebc8, review clean)
Task 2: implemented (commit d59b2956), review dispatched. Deviations declared: test helper insert order fixed (brief inserted credit_grants before its credit_accounts parent — FK violation), and `types: { bigint: postgres.BigInt }` added to the postgres() options (type-check fails TS2769 without it).
Ruling R7 (lockfile): the ~300-line pnpm-lock.yaml removal in d59b2956 stays. main's lockfile was stale, still pinning @google-cloud/speech / google-gax for agent-orchestrator after the dependency left that package.json; a root `pnpm install` reconciled it. Reverting by hand would leave the lockfile inconsistent with the manifests for every later task. Cost if wrong: if some untracked consumer still needs that tree, a later install/build fails loudly and the lines are restored by adding the dep back to the manifest that needs it.
Task 2: migration 0070 applied to the local test DB only; deployed environments still need it (rollout note, not a defect).
Note: the Opus session limit was hit at ~16:45 IST (resets 17:30 IST); the first Task 2 reviewer died on HTTP 429 mid-review. Re-dispatched on Sonnet. Model selection for the rest of the run stays at Sonnet until the limit resets.
Task 2: reviewer to check a named risk the interrupted reviewer surfaced — the replay check selects on idempotency_key with no tenant_id predicate, and unique(idempotency_key, seq) is likewise not tenant-scoped. Spec-verbatim, so plan-mandated if it is a defect.
Ruling R8 (Task 2, cross-tenant idempotency keys): the reviewer's Important finding is correct and I am fixing it rather than parking it, even though the code is verbatim from spec sections 2 and 4. The replay check matches on `idempotency_key` alone and `credit_ledger_key_seq_uq` is global, so an operator-chosen top-up key reused for a second tenant makes `spend_credits()` return that tenant's unchanged balance and apply no grant — a silent credit loss on the single writer of every balance. Every other identifier in this system is tenant-scoped, and invariant 5 ("one key, N rows") reads as per-account intent, so I am treating the global namespace as a spec defect, not a deliberate design choice. Fix: `where tenant_id = p_tenant and idempotency_key = p_key` in the replay select, and `unique(tenant_id, idempotency_key, seq)` on credit_ledger. The spec itself gets amended in the same commit — it is the binding authority and must not keep teaching the defect. Cost if wrong: if a caller ever legitimately depends on one key being global across tenants (no such caller exists in the plan), that call would double-apply; the fix is reverting one predicate and one index.
Task 2: carry into Task 9 — refundTask sums debit rows by idempotency_key and must filter on tenant_id too.
Task 2: minor (deferred): package.json `main: dist/index.js` points at nothing until Task 4 creates src/index.ts. Intentional.
Task 2: fix round 1/5 dispatched (resumed original implementer): tenant-scope the replay predicate and the unique index via a fix-forward migration 0071, add the cross-tenant same-key regression test and the multi-grant refund test, amend the spec. Reviewer's open ⚠️ (root `pnpm type-check` unaffected by the postgres.js bigint types option) to be resolved by the controller after the fix lands.
Task 2: fix round 1 landed (commit 355e48dd, 10/10 passing, 0 skipped; RED reproduced the silent cross-tenant credit loss before 0071). Ruling R9: the implementer's own concern — spec section 4's refund prose still shows an unscoped `where idempotency_key = 'task:{id}'` — is correct and gets amended in the same round; Task 9 implements refundTask straight from that prose, so leaving it would re-teach the defect one task later. Cost if wrong: a one-line doc edit.
Task 2: reviewer's ⚠️ resolved by the controller. `pnpm -r type-check` at the root: `packages/foundation/credits` passes, as do database, types, web, schema and the rest; the one failure is `@serverless-saas/idempotency` (TS2307 on `vitest` and `@serverless-saas/cache`) and it is pre-existing build-order, not this branch — `packages/foundation/cache/dist` does not exist, so the cache package's types are unresolvable until it is built, and nothing on this branch touches either package. Not a Task 2 finding.
Task 2: fix round 1/5 (3 addressed, 0 open; commits d59b295..7c282b7)
Task 2: complete (commits b21ebc8..7c282b7, review clean)
Task 3: implemented (commit 0c3f8f0a, 4/4 rate tests passing), review dispatched.
Ruling R10 (Task 3): the reviewer's Important finding stands — the brief's own four tests never distinguish round-once-after-summing from round-per-component, so the bug this module exists to prevent would pass green. Fixing rather than parking: the plan does not grade its own test coverage, and Task 4 builds on this module. Fix is one added test plus proof it fails against a per-component implementation. Cost if wrong: one extra test.
Task 3: fix round 1/5 dispatched (resumed original implementer).
Task 3: fix round 1/5 (1 addressed, 0 open; commits 0c3f8f0..16f9202)
Task 3: complete (commits 7c282b7..16f9202, review clean)
Task 4: implemented (commit efd1eb31, 18/18 passing, 0 skipped), review dispatched. Implementer found a real bug past the brief's literal code: drizzle-orm wraps postgres errors in DrizzleQueryError with the P0001 error on `.cause`, so the brief's `err.message` check never matched; spend.ts unwraps `.cause` first. Also flagged, unfixed: the brief's fixtures reuse tenant UUID ...a2 as read.integration's TENANT and spend.integration's TENANT_B on one shared test DB.
Ruling R11 (Task 4): the colliding tenant UUID is brief-mandated test data, so the fix goes in vitest.config.ts (`fileParallelism: false`) rather than in the test bodies. It also protects the integration test files Tasks 5, 7, 12, 13 and 14 add to this same package. Cost if wrong: the credits package's test files run serially, costing a few seconds.
Task 4: minor (deferred): getLedger's `opts = {}` default relaxes the brief's signature (beneficial).
Task 4: minor (deferred): no test exercises spend.ts's unwrapped-PostgresError fallback path (the branch for a driver that throws without a DrizzleQueryError wrapper).
Task 4: fix round 1/5 dispatched (resumed original implementer).
Task 4: fix round 1/5 (1 addressed, 0 open; commits efd1eb3..97b0ee0)
Task 4: complete (commits 16f9202..97b0ee0, review clean)
Task 5: implemented (commit ef84abf2, 21/21 in the credits package incl. 3 new isUnlimited tests; seed verified idempotent, 8 rate rows, no duplicates on re-run), review dispatched. Declared deviation: the brief's isUnlimited SQL joins `pe.plan = s.plan`, which errors because plan_entitlements.plan is text while subscriptions.plan is an enum; implementer added `s.plan::text`.
Task 5: minor (deferred): no test exercises the tenant_feature_overrides branch of isUnlimited, though the docstring promises an override wins over the plan entitlement.
Task 5: minor (deferred): seedCreditRates' select-then-insert is not race-safe under concurrent invocation; the credit_rates_subject_version_uq index turns a race into a constraint violation rather than a duplicate, so the invariant holds via a crash. Same pattern as features.ts, pre-existing.
Task 5: complete (commits 97b0ee0..ef84abf2, review clean)
Task 6: implemented (commit 1838b418, 4/4 in cost.test.ts; orchestrator suite 136 passing with 2 pre-existing unrelated failures), review dispatched. R5 resolved via route A: `resolveRate` used directly, since cost.ts already imported `db` from @serverless-saas/database for persistCost, so Drizzle was already proven to boot in the orchestrator. package.json / vitest.config.ts / pnpm-lock.yaml changes are authorized by R5.
Note: the Task 6 implementer used `git stash` to compare against a clean tree. The shared stash stack is a hazard across worktrees; verified afterwards that the stash list is empty and the tree is clean. Future dispatches should say so explicitly.
Task 6: both reviewer ⚠️ items resolved by the controller. (a) The 2 orchestrator test-file failures are pre-existing and unrelated: both are collection errors, `Failed to resolve entry for package "@serverless-saas/ai"` via src/mastra/tools/retrieveDocuments.ts:3 — the same unbuilt-workspace-package class as the idempotency/cache type-check failure, nothing to do with credits. Suite is 135 passed | 1 skipped; the 1 skipped is Task 6's own DB-gated cache test, which confirms the gating works as designed. (b) There is no CI in this repo at all — no .github directory — so the question of whether CI exports TEST_DATABASE_URL has no answer yet.
Ruling R12 (Task 6): park the reviewer's Important finding — the only test exercising the new rate-cache path is DB-gated and would skip silently in a CI that does not export TEST_DATABASE_URL. Real, but there is no CI to wire it into today, and the shipped code was verified correct by hand (all three seeded Gemini rates reproduce the fallback map's USD figures exactly through the new conversion path). Recorded as a rollout requirement instead: whoever stands up CI must export TEST_DATABASE_URL for the credits and agent-orchestrator jobs, or every integration test in this feature goes green by skipping. Cost if wrong: a future regression in loadRates/resolveRate/cache lookup ships unnoticed until it shows up in billing.
Task 6: minor (deferred): a bare model id `"ollama"` (no slash) now resolves to the seeded ollama row and prices free, where pre-diff it fell through to flash pricing. Reviewer checked every call site — only literal `gemini-*` and `ollama/<model>` ids are ever passed — so the path is unreachable today. A one-line guard removes the latent trap.
Task 6: minor (deferred): loadRates() awaits its five resolveRate calls sequentially; partial-failure semantics (early subjects cached, later ones not) are explained only in the report, not in a comment in cost.ts.
Task 6: complete (commits ef84abf2..1838b41, 1 parked, 2 minors deferred)
Task 7: implemented (commit 2c7073e5; dry-run wrote nothing, real run granted 200 credits, second run granted nothing with ledger count and balance unchanged), review dispatched. Implementer found and fixed a real bug outside this task's scope: packages/foundation/credits/src/spend.ts passed a raw Date into a drizzle sql template, which postgres.js cannot serialize, so EVERY grant carrying an expiresAt failed — Task 4 shipped a grantCredits that could not create an expiring grant and its tests never caught it. Task 13's trial and subscription grants both depend on this. Reviewer asked to check whether a regression test now covers it.
Ruling R13 (Task 7, expires_at vs billingCycle): fix, not park. The brief's sample code computes end-of-calendar-month for every tenant, but subscriptions.billingCycle is monthly|annual and there is no current_period_end column, so cycle end must be derived from billingCycle + startedAt (next anniversary strictly after now, clamped for short months). Left as-is, every annual tenant's opening grant expires within 31 days and they hit INSUFFICIENT_CREDITS mid-cycle — the exact failure this one-shot script exists to prevent. Cost if wrong: a wrong cycle end is a billing bug; the script is idempotent per tenant, so a correction requires a new key, not a re-run.
Ruling R14 (Task 7, missing-entitlement branch): track those tenants separately, print them as their own section, and exit non-zero when non-empty — but finish granting everyone else first. A one-shot script that reports success while leaving a tenant at zero credits is the failure mode the task exists to prevent. Cost if wrong: a noisier exit code on a data problem that would otherwise be silent.
Ruling R15 (Task 7, regression test): the spend.ts Date fix gets an integration test calling grantCredits with a real expiresAt, proven to fail against the pre-fix code. Task 4's tests missed this because they only ever created expiring grants through hand-written SQL. Task 13 depends on the same path. Cost if wrong: one extra integration test.
Task 7: cleanup ordered — the real run left a 200-credit balance and a backfill ledger row on tenant ...a4, which is also FREE_TENANT in unlimited.integration.test.ts:9. Fixture pollution in the shared test database.
Task 7: minor (deferred): the `pe.plan = s.plan::text` cast at backfillCredits.ts:56 may be unnecessary noise.
Task 7: minor (deferred): a single tenant's failure aborts the whole run; defensible fail-fast for one-shot code, safely resumable via idempotency, but undocumented as deliberate.
Task 7: rollout item — the reviewer could not verify from the diff whether every real plan has a `credits` plan_entitlements row in production. Confirm before the one-shot run, or R14's non-zero exit will fire on cutover.
Task 7: fix round 1/5 dispatched (resumed original implementer).
Task 7: fix round 1/5 (4 addressed, 0 open; commits 2c7073e..1f8c857)
Task 7: complete (commits 1838b41..1f8c857, review clean). Re-reviewer independently re-derived the cycle-end arithmetic: Jan-31 monthly clamps without drift (day reclamped from startedAt, never chained), annual Feb-29 lands Feb-28, future startedAt and exact-boundary cases both step correctly.
Ruling R16 (Task 8, chat overdraft — spec open question 4): implement the pre-check as `balance_micro > 0`, exactly as the brief specifies; build neither mitigation. A p95-threshold pre-check needs production turn-cost data that does not exist yet, and a per-tenant concurrent-stream cap is a separate feature with its own failure modes. Exposure is N x turn cost for N concurrent streams, the ledger records it faithfully, and ops can correct with a grant. Cost if wrong: a tenant can go modestly negative across simultaneous turns until one of the mitigations ships.
Task 8: dispatched (meter chat: fail-closed pre-check, post-turn debit, and the usage_records row chat has never written).
Task 8: first implementer died on a Sonnet session limit (HTTP 429, resets 21:50 IST) mid-implementation, leaving uncommitted work in the tree: src/credits.ts (116 lines), src/__tests__/credits.test.ts (22 lines, incomplete), a scratch src/__verify_task8__.ts (61 lines, must not be committed), and edits to routes/chat.ts and routes/chatStream.ts. Nothing committed. Re-dispatching a fresh implementer to audit and finish that work rather than starting over.
Task 8: implemented (commit 80799a57; 3 new tests, orchestrator suite 138 passed | 1 skipped vs a 135|1 baseline). Scratch verify script deleted, tree clean. CAVEAT carried into review: the replacement implementer kept the dead first implementer's credits.ts, credits.test.ts and both route diffs essentially unchanged, so the committed code was never observed to fail a test. Reviewer explicitly asked to judge whether the three tests discriminate at all, especially on the fail-closed and unlimited-skip paths.
Task 8: review of 80799a57 returned Approved with 2 Important coverage findings and no Critical. Reviewer independently traced fail-closed (credits.ts:34-48, no catch path returns allowed:true), unlimited-skip, the unconditional usage_records write (chatStream.ts:405), debit key/job_type/tenancy, pre-check placement ahead of the metered work, and confirmed checkMessageQuota/checkTokenQuota still exist for Task 11. Also confirmed exposure is not made worse than N x turn cost (R16 holds).
Task 8: fix round 1/5 dispatched (resumed implementer) — combines the user's instruction to prove the tests discriminate by mutation with the review's two Important findings: (A) debitChatTurn has zero unit coverage, its unlimited-skip and exact spend_credits call shape resting only on a deleted verify script; (B) the inherited tests were only ever observed to pass. Also ordered: a real integration test for BALANCE_QUERY's joins against the seeded schema (replacing the deleted transcript with standing evidence), a truthful name for the unlimited test, and an actionable log message for the rate-not-found path.
Task 8: minor (deferred): unlimited detection now lives in three places — BALANCE_QUERY's inline SQL, read.ts's isUnlimited, and debitChatTurn's separate call. Justified by the raw-pg test seam; latent drift risk if the credits feature's join shape changes.
Task 8: fix round 1/5 (5 addressed, 0 open; commits 80799a5..740460f). Re-reviewer verified the mutation evidence rather than accepting the list: all five were narrow single-line behavioral flips with quoted failure output, and mutation 3 (fail-open) is asserted on a THROWN balance read via mockRejectedValue, not a zero-balance branch — the correct scenario. Test seam is additive; the chatStream call site passes no deps, so production behavior is unchanged.
Task 8: complete (commits 1f8c857..740460f, review clean)
Task 9: implemented (commit 6b357cb4; 166 passing in the orchestrator, 30/30 in credits.test.ts, same 2 pre-existing collection failures). 4 of 5 mutations caught by tests; mutation 5 (402 mapping) could not be exercised because tasks.ts/tasks.execution.ts transitively hit the same pre-existing @serverless-saas/mcp collection failure that blocks tasks-execute/tasks-plan, so it was verified by code reading. Review dispatched with four declared concerns to judge: the 402 test gap, fail-closed at all three of charge/settle/refund (a failed settle or refund is not obviously the same decision as a failed charge), duplicate rate round trips, and the resume-endpoint settle recomputing the estimate from the current task_steps count rather than the amount originally charged.
Ruling R17 (Task 9, resume settle baseline): fix. tasks.ts:212 recomputes the settle baseline from the current task_steps count and a freshly-fetched model, but settleAmount = estimateMicro - actualMicro is only meaningful against what was actually debited at submit. A step added or a model changed in between silently credits or debits an amount nobody was charged. Fix: sum the tenant-scoped `task:{taskId}` debit rows in credit_ledger and settle against that, taking rate_id/rate_version from those rows so the settle is priced with the rate the charge used; no debit rows means nothing to settle. Cost if wrong: settles priced against a historical rate rather than the current one, which is the intended semantics anyway.
Ruling R18 (Task 9, failed refund): keep the swallow — a throwing refund must not break the failure path it already runs on — but make the loss loud and hand-recoverable: an error-level log naming it an unrefunded charge and carrying tenantId, taskId, idempotency key and amount, modelled on Task 8's UNBILLED CHAT TURN line. The refund key is idempotent per tenant, so a human replaying it later is safe. No retry queue; that is separate work. Cost if wrong: refunds still depend on someone reading logs.
Task 9: minor (deferred): duplicated resolveRate round trips — up to 4 per submission, plus 2 spendCredits calls relying on replay-safety. Matches existing precedent; no cache exists anywhere in the file.
Task 9: minor (deferred): the charge test does not assert rateId/rateVersion pass-through.
Task 9: minor (deferred): refundTask's "skip if a settle exists" guard is coarser than "skip if a settle already zeroed the charge". Unreachable today — settle and refund sit on mutually exclusive branches — but it will matter to a future caller.
Task 9: two open questions handed to the implementer to investigate: whether /api/workflows/execute's path in tasks.workflow.ts (untouched by this task) ever refunds a failed run that was charged at tasks.ts:228, and whether /api/tasks/execute's non-doc branch invokes runMastraTaskSteps exactly once after the route-level charge.
Task 9: fix round 1/5 dispatched (resumed original implementer).
Task 9: fix round 1/5 (5 addressed, 0 open; commits 6b357cb..dbc0387). Re-reviewer independently verified the settle-from-ledger query is tenant-scoped, sums ALL debit rows (multi-grant charges write several rows under one key, partitioned by seq — summing only the first would under-settle), returns early on no rows, and takes rate_id/rate_version from the charge rather than a fresh lookup. Also independently traced the two untestable paths (workflow refund, 402 mapping) and agreed the logic is correct.
Ruling R19 (Task 9, authorized scope growth): the tasks.workflow.ts change stands, including the unauthorized double-status-update fix. Q1 turned out to be a real gap — /api/workflows/execute charged and never refunded a failed run. The re-reviewer confirmed the double-status fix is a necessary consequence rather than opportunism: onStepFail's `failed` status was always overwritten by a trailing unconditional `completed` POST, so wiring the refund at that spot without the early return would ship a refund next to a status line contradicting it. ~18 lines, serves Task 9's own charter. Cost if wrong: a workflow run's terminal status is set by a path this plan did not originally cover.
Task 9: complete (commits 740460f..dbc0387, review clean)
Task 9: rollout item — the earlyTermination/onStepFail-only branches in tasks.execution.ts never call the task-level /fail endpoint, so a charged estimate on those paths stays un-refunded until something else calls it. Pre-existing, correctly left untouched, but it is a real money path.
Tasks 10+11: implemented (commits 5b14ea59, 70beea56). Gate grep clean — only definitions, their own log strings, design-rationale comments and the two named test files; no surviving route caller. Controller re-ran the grep independently after the deletion: only three comment references remain (credits.ts:14,39 and read.ts:17), no code. Collection failures dropped from 2 files to 1 (deleting tasks-plan.test.ts's quota block removed one importer of the unbuilt @serverless-saas/mcp chain). Suite 173 passed before and after.
Ruling R20 (Task 10, idempotency key): the brief specifies key `document:{documentId}`, but the route it names (documents.ts:170, which is /api/tasks/plan) has no documentId in scope — only taskId. The implementer used `task:{taskId}` and documented the mismatch. That is right: the key must identify the thing actually being charged, and settleTask reads back the `task:{taskId}` debit rows to compute its delta, so a `document:` charge key with a `task:`-shaped settle lookup would find no rows and silently skip the settle. jobType stays 'document', which is what the spec's job_type triple actually distinguishes. Cost if wrong: document charges are keyed like task charges; they remain per-tenant unique and jobType still separates them in the ledger.
Tasks 10+11: review returned Task 11 clean (deletion surgical, usage.ts's other exports intact, no QuotaCheckResult anywhere, and the reviewer confirmed credit_rates.resource_type='llm_tokens' is a distinct namespace from the retired features.key='llm_tokens', so rate resolution was not collaterally broken). Task 10 needs one fix.
Ruling R21 (Task 10, refund on planning failure): fix. documents.ts:220-226 (Mastra throws, 502) and :239-242 (empty output, 502) both leave the up-front charge unreconciled — no settle, correctly, but no refundTask either, so every planning failure permanently overcharges for zero delivered work. The implementer's comment conflated the brief's "leave the estimate as the final charge" (which covers a SUCCESSFUL run reporting no totals) with a run that fails outright; tasks.ts:173-177,224-226 refunds exactly these semantics. Cost if wrong: a planning failure refunds an estimate it arguably should keep, which is the safer direction.
Tasks 10+11: controller resolved the reviewer's ⚠️ — /api/tasks/plan (documents.ts) calls recordUsage nowhere, before or after this diff. recordUsage lives only in chatStream.ts:406, tasks.execution.ts:171,238 and tasks.ts:202. So the document-planning path is credit-metered but writes no usage_records row. Pre-existing, outside both briefs' file lists, and NOT introduced by Task 10 — recorded as a rollout item rather than a task finding, because the spec's stated goal is that every metered action also lands in the raw usage layer.
Tasks 10+11: minor (deferred): agentId is not in documents.ts:161's required-field guard, so an empty-string actorId can reach a money-moving call. Pre-existing lax validation.
Task 10: fix round 1/5 dispatched (resumed original implementer).
Ruling R22 (model selection): user directed that all remaining reviews run on Opus — Tasks 10 (re-review), 12, 13, 14, 15 and the final whole-branch review. Implementers stay on Sonnet. Reviews ran on Sonnet from Task 2 onward only because the first Opus reviewer died on a session limit at ~16:45 IST; that fallback is now retired. If an Opus limit fires again, fall back to Sonnet to avoid stalling and say so explicitly rather than quietly.
Task 10: fix round 1/5 (1 addressed, 0 open; commits 70beea5..890c64e). Opus re-reviewer verified refundTask's new override seam preserves every existing caller byte-for-byte (grepped all five: tasks.workflow.ts:95, tasks.ts:176,225, tasks.execution.ts:165,230 — none passes an override), confirmed the settle race was real (void settleTask yields at its first await with nothing written, so the refund guard would essentially always miss and double-adjust), and confirmed awaiting introduces no new throw path (settleTask's body is fully try/catch-wrapped and getPool() is memoized earlier in the same request).
Task 10: the implementer's self-declared "out of scope" residual was adjudicated by the reviewer rather than accepted: a zero-delta settle writes no row, so an empty-output refund could return a charge that exactly matched usage — but it needs generate() to succeed, return falsy text, AND cost exactly the estimate (inputTokens + 4*outputTokens === 8000 at seeded rates), and the free-rate row short-circuits on refundTask's net >= 0n guard. Minor, not reopened.
Tasks 10+11: minor (deferred): refundTask's body docstring at credits.ts:381,385,398 still says it sums the `task:{taskId}` row and keys on `task:{taskId}:refund`, no longer universally true with the override seam. The per-field JSDoc at :369-374 is correct. Documentation drift only.
Tasks 10+11: minor (deferred): documents.ts:259-268's comment implies the settled-row guard fully covers "already reconciled"; it does not mention the zero-delta hole.
Tasks 10+11: rollout item — documents.ts:274-277's JSON-parse-failure branch returns 502 with no refund, unlike the two branches R21 covered. settleTask has usually written a row by then so refundTask would no-op, but the asymmetry is unstated. Pre-existing, outside the fix diff.
Task 10: complete (commits dbc0387..890c64e, review clean)
Task 11: complete (commit 70beea56, review clean)
Task 12: implemented (commit c83f432b; 63 passing = 45 baseline + 18 new; six mutations all caught, including BigInt-serialization dropped and tenant scoping taken from a query param instead of the JWT claim). Controller independently verified the mount: api.route('/credits', creditsRoutes) at app.ts:179, below usageRecordingMiddleware at :161, in the same block as /billing and /entitlements — all ten secure middleware apply. Review dispatched on Opus per R22, with the seed changes flagged: the diff touches seeds/permissions.ts and seeds/role-permissions.ts, and a role that should not grant itself credits would be Critical.
Ruling R23 (Task 12, credits:create on tenant admins): exclude `credits:create` from both `owner` and `admin` in role-permissions.ts, the way entitlements:* is already excluded from admin. The brief mandates "admins get both", but role-permissions.ts gives owner ALL_PERMS and admin everything but tenant:delete and entitlements:*, so as committed a customer's own admin could POST /credits/grants with an arbitrary amountMicro and grantType 'purchase' and mint unbounded credits against a metered billing system. Granting credits is an ops action; the ops portal has separate auth. Seed change only — the permission stays defined and the route guard stays as-is. A test asserts neither role carries it, so widening ALL_PERMS later cannot silently restore it. Cost if wrong: an ops-portal or automated grant path that authenticates as a tenant owner would 403 and need the permission restored, one seed line. USER-VISIBLE PRODUCT DECISION — surface in the final summary.
Ruling R24 (Task 12, unvalidated query params): fix. ?count=abc reaches BigInt(NaN) and throws RangeError; ?limit=abc reaches .limit(NaN); ?before=garbage puts an Invalid Date into a SQL comparison; ?kind=nonsense hits the ledger enum cast. Four user-controlled 500s on routes that already import zod and use it correctly for the grant body. Cost if wrong: none, it is pure input validation.
Task 12: fix round 1/5 dispatched — also covers the tenant-isolation proof covering only 1 of 4 routes (mutation 5 proves less than the report claimed), the ledger pagination assertion being expect.any(Object), the count-defaults-to-zero estimate, and forbidden(c: any).
Task 12: minor (deferred): no nextBefore/hasMore cursor on the ledger response though `before` is accepted as input; TS18046 in the test file (matches three existing files' convention); POST /credits/grants returns 200 rather than 201.
Task 12: rollout item — these routes 403 in any deployed environment until seedPermissions and seedRolePermissions are re-run there. The seed run must precede or accompany the Lambda deploy.
Task 12: fix round 1/5 (6 addressed, 0 open; commits c83f432..8c45542). Opus re-reviewer verified the permission exclusion is a single string-equality filter that drops only credits:create — credits:read survives on owner, admin and member — and that role-permissions.test.ts asserts against the materialized ROLE_PERMISSIONS object rather than restating the filter, so widening ALL_PERMS later fails it. Also verified the vitest.config widening pulls in exactly one new test file whose only db import is type-only (erased at compile time), and that the zod defaults short-circuit on undefined so a missing param never becomes NaN.
Task 12: minor (deferred): zod's .datetime() rejects UTC offsets (+05:30) and local times, accepting only Z-suffixed strings, so a client emitting offset timestamps now gets a 400 where new Date() previously accepted it; `{ offset: true }` closes it. Consistent with grantSchema's existing expiresAt validation.
Task 12: minor (deferred): an explicitly empty param (?count=, ?limit=) coerces to 0 rather than falling to the default, so ?count= estimates zero and ?limit= returns an empty page. Pre-existing for limit; a regression only against the new count default. `.min(1)` on limit plus treating '' as absent closes both.
Task 12: complete (commits 890c64e..8c45542, review clean)
Tasks 13+14: implementer stalled (stream watchdog, 600s no progress) just before committing. Work left uncommitted in the tree, HEAD still 8c455424. Resumed the same agent to finish. Three things flagged for it to resolve, visible from git status: (a) two untracked handler files for one handler — apps/worker/src/creditsExpireHandler.ts and apps/worker/src/handlers/creditsExpire.ts; (b) a new packages/foundation/database/cycleEnd.ts alongside a modified scripts/cycleEnd.ts, which risks duplicated billing-cycle date arithmetic when the instruction was to reuse Task 7's; (c) template.yaml and both Makefiles modified, i.e. a new Lambda function beyond the brief's file list, whose handler path must match whichever file survives (a).
Ruling R25 (Task 13, CRITICAL — unbounded credit mint): fix. billing.ts:119 calls the cycle grant from upgradeHandler, which inserts a NEW subscription row with a fresh uuid on every call, so the key sub:{newSub.id}:0 is fresh every time and the replay check never fires. No same-plan guard, no payment gate (the route still carries a TODO to wire one), and the prior grant stays live. Anyone with billing:update can loop POST /billing/upgrade and accumulate 10,000 credits per call. Fix: key on (tenant, plan, cycle start) so repeated changes within a cycle collapse to one grant, and return early when plan and billing cycle are both unchanged. A genuine mid-cycle plan change still granting a fresh allowance is accepted for v1 — proration and clawback need a billing engine and are out of scope. Cost if wrong: a tenant changing plans mid-cycle gets one extra allowance, bounded, instead of unbounded.
Ruling R26 (Task 13, cycle default): remove the `= 0` default on the cycle parameter. The reviewer identified the inverse failure mode: today the constant over-grants, but once a recurring engine reuses one subscription id across cycles, sub:{id}:0 replays silently — spend_credits() returns the unchanged balance with no error, no grant and no log, so a paying tenant stops receiving credits invisibly. Removing the default makes that a compile error when the engine lands. Cost if wrong: one extra argument at each call site.
Task 13/14: ROLLOUT BLOCKER — nothing in the codebase grants subscription credits recurringly. grantSubscriptionCycleCredits has exactly one caller and it is a plan-change handler, so a metered tenant gets 200 trial credits, the sweep zeroes them after 14 days, and no cycle grant ever fires again unless they change plans. Treated as a rollout blocker rather than a task defect because building a recurring-billing engine is outside this plan's scope. MUST reach the user in the final summary.
Task 13/14: reviewer established a sharper invariant for the sweep than the spec states — spend_credits() already takes `for update` on the ACCOUNT row unconditionally before the expiry loop, so every caller through the function is serialized per tenant; the grant-row lock only protects against a second code path that mutates credit_grants without holding the account lock. So the property to guard is "the worker never grows its own grant-mutating query", which is structurally checkable rather than concurrency-testable. Ordered into the handler's header comment.
Task 13/14: minor (deferred): rowsOf duplicated in five places; unused Upstash env vars on the new SAM function; rate(1 day) fires 24h after deploy at an arbitrary hour rather than nightly (cron(0 3 * * ? *) would match the description, and the brief did specify rate(1 day)); the no-op initSecrets() call in creditsExpireHandler.
Task 13/14: fix round 1/5 dispatched.
Task 13/14: fix round 1 (5 addressed, 1 new Important; commits acb954f..aa2b203). Opus re-reviewer derived the substituted key scheme from the code and walked every ordering — repeat upgrade, monthly/annual toggle, downgrade-then-re-upgrade, genuine mid-cycle change, new-cycle call, first-ever grant, a lookup finding another plan's row, and tenant poisoning of the lookup (impossible: planting a subscription grant needs credits:create, which R23 withheld from owner and admin). All sequential vectors closed; it accepted the ledger-anchored key over the literal early-return I had ruled for.
Ruling R27 (Task 13, concurrent-burst key race): the fresh-cycle branch computes expiresAt from `new Date()`, preserving milliseconds, so N concurrent requests in the pre-commit window each derive a distinct key and each mint a full allowance — at every cycle boundary and at a tenant's first subscription grant. The account lock serializes but cannot collapse them, because the replay check is on (tenant_id, idempotency_key). Fix: quantize only the key's period component to the UTC date (.slice(0, 10)) while still storing the full expiresAt on the grant; racers converge on one key. One plan cannot have two cycle ends on the same UTC date, so no legitimate grant is lost. Cost if wrong: none identified — the truncation affects only key identity.
Task 13/14: minor (deferred): startedAt < lastExpiry compares subscriptions.started_at (timestamp without time zone) against credit_grants.expires_at (timestamptz) — identical under a UTC host, the deployed default, but skews by the offset on a non-UTC one. Pre-existing schema, newly load-bearing.
Task 13/14: minor (deferred): concurrent upgradeHandler calls cancel-and-reinsert with no lock and can leave a tenant with several status='active' subscription rows — the invariant the read.ts ordering alignment now defends against. Pre-existing.
Task 13/14: rollout item — POST /billing/upgrade still has no payment gate (the route retains its TODO), so after a genuine cycle end any holder of billing:update can self-serve one allowance per cycle by re-POSTing their current plan. Equal to what a legitimate renewal grants, so not an over-mint, but it is the only replenishment path in the codebase and it is ungated by payment.
Task 13/14: fix round 2/5 dispatched.
Task 13/14: fix round 2/5 (3 addressed, 0 open; commits aa2b203..9d5d89a). Opus re-reviewer proved the truncation cannot collapse a legitimate grant: expiries are monotone non-decreasing (the reuse branch pins to lastExpiry; the fresh branch only fires at startedAt >= lastExpiry and steps at least a clamped calendar month, minimum 28 days), so two distinct periods can never share a UTC date, and day-of-month clamping cannot make successive period ends coincide. Convergence verified against 0071:44-60 — the account `for update` precedes the replay check, so racers on an identical key serialize and the later ones return early with no grant row.
Task 13/14: complete (commits acb954f..9d5d89a, 3 minor observations recorded)
Task 13: minor (deferred): the truncation test recomputes the expected key with the same function and the same .slice(0,10) the implementation uses, so it pins the key's shape rather than the convergence property. It does fail on the un-truncation mutation, so the evidence is honest, but the reviewer named a practical deterministic replacement: grant with startedAt = t, capture the key, wipe the tenant's rows, grant again with startedAt = t + 5ms so both calls provably take the fresh branch, and assert the two keys are EQUAL. Timing-independent, no second connection, fails on un-truncation without restating the format string.
Task 13: minor (deferred): the report's claim that no fixture can force a created_at tie is false — created_at is defaultNow() and a test can simply update it to a literal. The conclusion survives for a different reason: credit_grants.id is defaultRandom(), so `id desc` picks arbitrarily between tied rows and a test could only pin which random uuid wins. NOTE for a future editor: the semantically load-bearing tie-break would be `expires_at desc` — on a genuine tie, picking the row with the smaller expiry flips stillInPriorCycle to false and mints an extra allowance, exactly what this key scheme exists to prevent. R27 specified id desc and it was implemented as ruled.
Task 13: minor (deferred): the multi-plan test pins "once per distinct plan" but not the "no clawback" half — all three filters are `like 'sub:{tenant}:{plan}:%'`, and a clawback would write its reversal under a different key (an adjust or expiry row) that no assertion would see. Asserting the tenant's balance_micro equals 200 + 2,000 + 10,000 credits would pin both halves in one line.
Ruling R28 (Task 15, CRITICAL — gate on an uncharged surface): the approve control was mounted on CreateTaskDialog, which posts to /api/v1/tasks (products/agent-platform tasks.create.ts) — a route with no credit logic. chargeTaskEstimate runs from the orchestrator's /api/tasks/execute, whose only caller is the SQS worker taskWorker.execution.ts:30, fired when a task begins EXECUTING. So approving authorized nothing, the 402 branch was unreachable outside its own mock, and a zero-balance tenant would create the task and fail silently in the worker later. Ruling: investigate whether a user-triggered execution surface exists (a board status transition to Todo, or an explicit run control); if so move the estimate-and-approve there, since that is where credits are committed. If execution is enqueued with no human action in between, an approve gate is not implementable today — strip the Approve/Cancel semantics and keep only an informational cost preview whose copy says it is an estimate of what the task will cost when it runs, not an authorization. Either way delete the unreachable 402 branch and its test. Cost if wrong: the gate lands on a transition the product later moves, or the preview under-promises.
Ruling R29 (Task 15, CRITICAL — wrong rate estimated): the dialog requested tool_call/task.create, seeded at per_call_micro 0, while the real charge resolves llm_tokens at rateSubjectFor(model) sized by stepCount x AVERAGE_STEP_TOKENS. So it rendered "0 credits" for a task that debits a real amount and reported sufficient: true at any balance. Ruling: estimate llm_tokens against the agent's model and step count, matching estimateTaskMicro's inputs; if neither is knowable at that point in the UI, show no figure at all — state that a cost applies and show the balance. A wrong number is worse than none, and zero is the worst wrong number available. Cost if wrong: the preview is less precise than it could be.
Ruling R30 (Task 15, dead idempotencyKey): remove it from the payload and remove the test asserting it, leaving a comment that create-side idempotency is unimplemented. Confirmed independently that no route reads it and that createTaskSchema is non-strict so it is silently stripped. It is harmless on the wire but misleading as a claim: the real charge key is task:{taskId} with a fresh taskId per create, so two clicks produce two tasks and two debits — replay-safety is not achieved for the operation the UI implied it made safe. Cost if wrong: a future create-side idempotency feature re-adds one field.
Task 15: fix round 1/5 dispatched. Also covers the browser checklist's steps 3 and 4, which mispredict their own outcomes for reasons already in the implementer's report, and a stale-payload window where the form stays editable after approve.
Task 15: minor (deferred): four self-asserting shape tests in useCredits.test.ts construct object literals and assert their own fields — tsc already enforces the narrowing; no retry affordance after the insufficient state; no aria-live on state transitions (consistent with UsageBar.tsx).
Task 15: fix round 1 (5 addressed, 2 new Important; commits 7e8f137..8b1d79f). Opus re-reviewer independently traced the new charge path and confirmed it is real: PlanReviewPhase.tsx:53 -> useTaskMutations.ts:77 -> tasks.approval.ts:83 (the repo's only execute_task publisher) -> taskWorker.execution.ts:30 -> tasks.ts:67-77 chargeTaskEstimate. Also confirmed the estimate matches the charge input-for-input, including that the UI reads llmProvider.model from the same table fetchAgentModelSelection uses rather than the agents.model display column.
Ruling R31 (Task 15, duplicate approve path): SidebarActions.tsx:22-27 renders a SECOND, ungated "Approve Plan" under the same awaiting_approval condition, wired to the same mutation, and TaskDetailView renders both together — so the gate is bypassable one click away and PlanReviewPhase.test.tsx's assertion passed only because it rendered the phase in isolation. Ruling: one approve path — route the sidebar through the gate or hide it while a plan awaits approval, plus a test rendering the composition rather than the phase alone. Note tasks.approval.ts:41 also accepts status 'blocked', which the phase card never renders, so a blocked task is currently approvable only via the ungated button; the fix must not leave that as the survivor. Cost if wrong: the sidebar loses a shortcut.
Ruling R32 (Task 15, error branches remove the approve control): an estimate failure must never block approval. TaskExecutionCost.tsx:56-62 and ApproveCost.tsx:73-85 render no actionable control when the agent lookup or the estimate fails, where Approve Plan was previously unconditional. The estimate is a preview and spend_credits() is the authoritative check — it fails closed and posts an "Insufficient credits" comment — so proceeding without a preview is safe. Render a working Approve alongside copy saying the cost could not be estimated. Cost if wrong: a user approves without seeing a cost, and the server-side check catches it.
Task 15: fix round 2/5 dispatched. Also covers deleting insufficientOverride and its manufactured-rejection test, the remaining idempotencyKey residue in ApproveCost, a tautological assertion at TaskExecutionCost.test.tsx:126, the checklist's broken numbering, and a miscounted test claim (apps/web had 25 files before, not 26; net is +1 file / +11 tests, no coverage lost).
Task 15: minor (deferred): hoist rateSubjectFor / AVERAGE_STEP_TOKENS / DEFAULT_TASK_MODEL into @serverless-saas/credits — both sides already depend on it, so the duplication is avoidable, and the web copy's tests pin it against itself so drift is silent in both directions; handleExecution sends pendingSteps while the UI estimates on steps.length (equal at first approval, divergent on a resume); handleGetAgent omits the orchestrator's is_platform OR tenant_id predicate.
Ruling R31 REVISED (user decision, 2026-08-29): delete the approve controls from SidebarActions.tsx entirely rather than routing them through the gate — user said "sidebar doesnt need approval". The gated PlanReviewPhase card becomes the only approve path. Implementer told to check whether a `blocked` task can carry a plan awaiting approval (tasks.approval.ts:41 accepts it, the phase card renders only for awaiting_approval), and to widen the card's condition if so rather than leave such a task un-approvable.
Task 15: user asked whether plan approval could move into the chat approval flow. Investigated: the stack exists — ApprovalCard.tsx, the approvalRequest message field, onApprove/onDismiss in MessageItem, an approval_request SSE event, persistence.ts:163-197, and messages.ts:169 accepting it — but it is DISABLED at MessageItem.tsx:284 behind a literal `{false && ...}` guard, and it is built for MCP tool-call approval (sessions.ts:65-66, `[mcp-approval]`), not plan approval. The sibling clarification flow (POST /api/chat/clarification, sessions.ts:152) IS live. Moving plan approval there means reviving dead-coded UI, repointing a tool-approval channel at a status transition, and relocating the credit gate onto that card — a separate feature with its own brief. User agreed to finish Task 15 first. FOLLOW-UP, not built.
Task 15: fix round 2/5 (all findings addressed, 0 open; commits 8b1d79f..3f7b06b). Opus re-reviewer independently re-derived the `blocked` question and agreed, on stronger evidence than the implementer gave: it read every status:'blocked' writer (taskWorker.planning.ts:63,98; taskWorker.execution.ts:49,57; watchdogHandler.ts:75,143; internal/tasks.steps.ts:169; internal/tasks.lifecycle.ts:75,108,114; tasks.actions.ts:50,113; tasks.approval.ts:87) and found none represents a plan waiting on a human — and, decisively, tasks.approval.ts:71's atomic update predicates on status = 'awaiting_approval' and 409s otherwise, so line 43's acceptance of 'blocked' is dead widening and such a task is un-approvable through the API regardless of UI. No user path was removed.
Task 15: complete (commits 9d5d89a..3f7b06b, review clean)
Task 15: minor (deferred): PlanApprovalSurface.test.tsx's exactly-one-approve invariant is enforced by literal label strings — getAllByRole with the exact name 'Approve' would not match a reintroduced button labelled 'Approve Plan', and the second assertion queryByText('Approve Plan') is what actually caught the mutation. A differently-cased or icon-only reintroduction would pass both.
Task 15: ROLLOUT/FOLLOW-UP — a third ungated approve path exists outside this diff: apps/web/app/[tenant]/dashboard/notifications/NotificationRow.tsx:69-81 renders an enabled "Approve" for any task.awaiting_approval notification, wired to notifications/page.tsx:80-82 calling PUT /api/v1/tasks/{id}/plan/approve directly — no estimate, no balance check, no cost card. Same defect class as R31 on a different screen, pre-existing. It makes the implementer's "the gated card is now the only approve control anywhere" claim false. The server-side check still fails closed, so it is a UX gap rather than a money leak. Decide: gate it, or remove the button and route the notification to the task detail page.
Task 15: pre-existing note: tasks.approval.ts:43 accepting 'blocked' is dead code contradicted by the atomic predicate at :71; such a request gets a 409 with the misleading message "Task plan cannot be reviewed in its current state" rather than the 400 line 43 implies.

## Final whole-branch review (Opus, 28 commits, 82 files)

Verdict: NOT READY — two blockers, both small and local. Core verified sound: spend_credits() is the sole writer of balance_micro (grep across the whole repo returned exactly one UPDATE), the per-tenant idempotency namespace is correct at all four key-based lookups, fail-closed holds at every site, and the settle reads its baseline back from the ledger.

BLOCKER A: tasks.execution.ts:227-240 — earlyTermination (set at :98, :107, :155-158, :213-217) leaves the estimate charged at :42 with no settle and no refund. A workflow returning status:'failed' — the ordinary failure shape, not a throw — takes this path. tasks.workflow.ts:79-88 already refunds in exactly this situation, so the branch is internally inconsistent.
BLOCKER B: migration 0071:60-109 — the lazy expiry sweep and the INSUFFICIENT_CREDITS raise share one implicit transaction, so a failed spend rolls back the sweep. REPRODUCED by the reviewer against credit_system_test: after the failed call, balance_micro unchanged, zero expiry rows, spent_micro still 0. Breaks invariant 3. At the seam: credits.ts:53's chat pre-check reads the stale balance and passes, debitChatTurn swallows the error at :131-133, and the tenant chats free until the nightly Lambda runs. The existing test at spend.integration.test.ts:84 only covers the succeeding spend, which is why fifteen scoped reviews missed it.
ALSO ORDERED: refund and settle grants pass no expiresAt (credits.ts:348-361, :433-442), so 0071:112-118 writes null and a refund converts expiring credits into permanent ones — contra spec §4's "shortest expires_at among the grants it came from".

Reviewer disagreed with the ledger's disposition on three deferred items, and I accept all three: (a) documents.ts writing no usage_records is NEW severity, not pre-existing — the path being unmetered was pre-existing, the path being CHARGED is new, and a charged action absent from the raw layer is a reconciliation hole; (b) the earlyTermination refund gap is likewise not pre-existing, since nothing was charged before this branch — promoted to Blocker A; (c) the bare `ollama` free-pricing case is a one-line guard in a pricing function, so it is a fix, not a deferral.

Invariants: 1, 4, 5, 6, 8 hold. 2 holds with a gap (refund/trial/subscription grants carry null actor attribution). 3 DOES NOT HOLD (blocker B). 7 CANNOT DETERMINE — no rate-edit path exists anywhere in the branch, so the versioned-immutable discipline is unimplemented and unenforced; nothing stops an in-place UPDATE of pricing_schema, which would break criterion 3 retroactively.
Acceptance criteria: 1, 4, 5, 10, 11 met. 2 met vacuously (no call site charges a flat rate). 3 met for debits. 6 met for chat, NOT for documents. 7 not met as written but the code follows spec §7 — the criterion contradicts the spec. 8 and 9 not met (blocker B). 12 cannot determine — backfill has never run anywhere.
Migration safety assessed independently: 0069 fully guarded and safe to re-run; 0070/0071 idempotent by construction; the index rebuild takes no meaningful lock on an empty table; ordering correct. Two caveats: meta/0070_snapshot.json was never committed (harmless to the migrator, confusing to the next drizzle-kit generate), and the run against Supabase must set DATABASE_DISABLE_PREPARE/pgbouncer=true since these have only touched a direct-connection local Postgres. Design note: credit_accounts.tenant_id has no ON DELETE, so tenant deletion now fails with an FK violation once an account row exists.
Final-fix agent dispatched for both blockers plus the expiresAt gap.
